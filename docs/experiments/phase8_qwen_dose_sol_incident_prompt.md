# Sol high incident adjudication — Phase-8 Qwen caption-dose chain

Read the live dose contract, queue, monitor state, latest deterministic and
Luna reports, execution manifest, log, checkpoints, disk state, and processes.
Return only the JSON required by the supplied schema. Decide whether evidence
is transient, safe for an identical-contract audited resume, or requires stop.
Do not execute any action. Scientific metric misses never authorize retry or
parameter changes. Codex review is always required.
