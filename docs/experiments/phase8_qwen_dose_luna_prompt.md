# Luna xhigh monitor — Phase-8 Qwen caption-dose chain

Monitor the live, predeclared 50k/100k paired caption-dose chain. Read the
authoritative live files under `/home/kojiek/MeanAudio`, state under
`/home/kojiek/logs/phase8_qwen_dose_monitor`, and outputs under
`/home/kojiek/exps_nvme/phase8_qwen_official_matched_dose`.

You have proposal-only authority. Never launch, stop, kill, resume, edit the
live repository, change parameters, or turn a metric miss into retraining.
Write any proposal only under `proposals/` in the isolated worktree. Codex must
review every proposal; stop/change/relaunch additionally requires Sol high.

Verify contract hash, exact active command, monotonic checkpoint iteration,
finite stable checkpoints, the 50 GiB disk floors, and the sequential gates:
20k report -> 50k control/Qwen audits -> 50k paired report -> 100k arms ->
100k paired report. A low CLAP score is report-only, never a failure trigger.

Report the active step, evidence, estimated progress, and proposal path, if any.
